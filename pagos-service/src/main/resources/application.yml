server:
  port: 0

spring:
  application:
    name: pagos-service

  datasource:
    url: jdbc:mysql://localhost:3306/pagos_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
